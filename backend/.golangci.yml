---
linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - gosec
    - misspell
    - revive
  disable-all: true

run:
  timeout: 5m
  working-directory: backend
